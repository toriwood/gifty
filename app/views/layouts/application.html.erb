<!DOCTYPE html>
<html>
<head>
  <title>GIFTED</title>
  <%= stylesheet_link_tag    'application', media: 'all', 'data-turbolinks-track' => true %>
  <%= javascript_include_tag 'application', 'data-turbolinks-track' => true %>
  <%= csrf_meta_tags %>
  <%= favicon_link_tag 'gift.png' %>
  <link href='https://fonts.googleapis.com/css?family=Josefin+Sans' rel='stylesheet' type='text/css'>
</head>
<body>

  <nav class="navbar navbar-default" role="navigation">
    <div class="container-fluid">
      <!-- Brand and toggle get grouped for better mobile display -->
      <div class="navbar-header">
        <button type="button" class="navbar-toggle" data-toggle="collapse" data-target=".navbar-ex1-collapse">
          <span class="sr-only">Toggle navigation</span>
          <span class="icon-bar"></span>
          <span class="icon-bar"></span>
          <span class="icon-bar"></span>
        </button>
        <%= link_to "#{image_tag("gift.png")} GIFTED".html_safe, root_path, class: "navbar-brand", style: "margin-top: -10px" %>
        <% if user_signed_in? %>
        <%= link_to "Your Feed", users_path, class: "navbar-brand", style: "margin-top: 5px" %>
        <% end %>
      </div>
  
      <!-- Collect the nav links, forms, and other content for toggling -->
      <div class="collapse navbar-collapse navbar-ex1-collapse">
        
        <%= form_tag(gifts_path, :method => "get", class: "navbar-left navbar-form") do %>
        <div>
          <%= text_field_tag :search, params[:search], class: "form-control", placeholder: "Find Gifts" %>
          <%= submit_tag "Submit", class: "btn btn-primary" %>
          </div>  
        <% end %>

        <ul class="nav navbar-nav navbar-right">
        
        <% if user_signed_in? %>
        <li id="notification_li">
          <a href="#" id="notificationLink" style="margin-top:5px;"><%= image_tag("people.png") %></a>
          <% if notifications.count > 0 %>
           <span id="notification_count"><%= notifications.count %></span>
          <% end %>

            <div id="notificationContainer">
              <div class="notificationTitle">Notifications</div>
              <% notifications.each do |n| %>
                <% if n[1][0] >= 1 %>
                <div class="notificationBody notifications"><%= "#{User.find(n[0].to_i).username.titlecase} will be celebrating soon! They'll be celebrating #{n[1][1].keys.first} in #{n[1][0]} days on #{n[1][1].values.first}."%></div>
                <% elsif n[1][0] < 1 %>
                <div class="notificationsBody notifications"><%= "#{User.find(n[0]).username} is celebrating today! They're celebrating #{n[1][1].keys.first}!" %></div>
                <% end %>
                <% end %>
              <div id="notificationFooter"><a href="#">See All</a></div>
            </div>
        </li>
        <% end %>

          <li class="dropdown">
            <a href="#" class="dropdown-toggle" style="margin-top: 5px;"data-toggle="dropdown">Browse <b class="caret"></b></a>
            <ul class="dropdown-menu">
              <li><%= link_to "Wishlists", wishlists_path %></li>
              <li><%= link_to "Gifts", gifts_path %></li>
            </ul>
          </li>

        <li class="dropdown">
            <a href="#" class="dropdown-toggle" style="margin-top: 5px;" data-toggle="dropdown">Browse by Interest <b class="caret"></b></a>
            <ul class="dropdown-menu">
              <% interests.each do |interest| %>
              <li><%= link_to interest.name, interest %></li>
              <% end %>
              <strong><li><%= link_to "View All Interests", interests_path, style: "padding-left: 20px" %></li></strong>
            </ul>
        </li>

        <% if user_signed_in? %>
          <li class="dropdown">
            <a href="#" class="dropdown-toggle" style="margin-top: 5px;" data-toggle="dropdown"><%=current_user.username %><b class="caret"></b></a>
            <ul class="dropdown-menu">
              <li><%= link_to "Profile", user_path(current_user) %></li>
              <li><%= link_to "Edit Account", edit_user_registration_path %></li>
              <li><%= link_to "Sign Out", destroy_user_session_path(id: current_user.id), method: :delete %></li>
            </ul>
          </li>
          <% else %>
          <li><%= link_to "Sign In", new_user_session_path, style: "margin-top: 5px" %></li>
          <li><%= link_to "Sign Up", new_user_registration_path, style: "margin-top: 5px" %></li>
          <% end %>

        </ul>
      </div><!-- /.navbar-collapse -->
    </div>
  </nav>

	<div class="container">
   	<% flash.each do |key, value| %>
      <div class="alert alert-<%= key %>"><%= value %></div>
    <% end %>
   </div>

  <div class="container <%= controller.controller_path %> <%= controller.action_name %>">
    <%= yield %>
  </div>
  
</body>
</html>
